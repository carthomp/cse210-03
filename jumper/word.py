import csv
import random

    
        
class Word():

    def __init__(self):

        with open ("word.csv") as csv_file:
            number
            count = 0
            for i in csv_file:
                count +=1
            
    def self.set_word(self):
        
        word = read_words("./words.csv")
        return word
    _
.
    def self.self.pick_word(sslfeself):
        
        choosen_word = random.choice(set_word())
    choosen_word = choosen_word[0].upper()
    return choosen_word
        self._    array = []
            
    return cself.hoosen_word
        self._    array = []
            
            with open(filename, "rt") as csv_file:
                
                reader = csv.reader(csv_file)
            
                next(reader)
    
                for row in reader:
                  
                    array.append(row)
                        
                
                    
            
            return array                